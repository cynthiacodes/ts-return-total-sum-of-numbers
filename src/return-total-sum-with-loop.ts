/**
 * 
 * @param inputArray - array of numbers and strings
 * @returns totalSum - the toatl of all numbers in inputArray
 */

function returnTotalSum(inputArray){
    return 0;
}

export default inputArray;